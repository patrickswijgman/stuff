{
  "scripts": {
    "build": "tsx app/main.ts",
    "watch": "nodemon --ext html,css,ts,md --ignore 'dist/**' --exec 'npm run build'",
    "serve": "serve dist -p 8000",
    "start": "npm run watch & npm run serve"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "globby": "^14.0.2",
    "marked": "^14.1.3",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.17.6",
    "nodemon": "^3.1.7",
    "serve": "^14.2.4"
  }
}
